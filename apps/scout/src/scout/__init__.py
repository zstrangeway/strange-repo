"""scout — a local-first, bring-your-own-model job search assistant.

Three capabilities, in `postings`, `tailoring` and `applications`. `cli` and
`mcp_server` are two surfaces onto them and hold no logic of their own.
"""
