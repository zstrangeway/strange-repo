{
  "name": "creed",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "task lint",
    "mcp": "task mcp",
    "sync": "task sync",
    "test": "task test"
  },
  "devDependencies": {
    "@go-task/cli": "^3.52.0"
  }
}
