version: '3'

tasks:
  dev:
    desc: Run the development server
    cmds:
      - pnpm exec next dev

  build:
    desc: Build for production
    cmds:
      - pnpm exec next build

  serve:
    desc: Run the production server
    cmds:
      - pnpm exec next start

  lint:
    desc: Lint the app
    cmds:
      - pnpm exec eslint

  test:
    desc: Run the Gherkin specs against a real browser, with gary-api stubbed
    cmds:
      - pnpm exec cucumber-js

  test:e2e:
    desc: Run the end-to-end specs against a real gary-api and a throwaway database
    env:
      GARY_E2E: '1'
    cmds:
      - pnpm exec cucumber-js --profile e2e
